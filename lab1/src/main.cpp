#include "presenter.h"
#include "gpxparser.h"
#include <QApplication>

int main(int argc, char *argv[])
{
    QApplication a(argc, argv);

    Presenter p;
//    MainWindow w;
//    w.show();
    return a.exec();
}
